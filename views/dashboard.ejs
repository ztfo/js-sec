<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Dashboard</title>
    </head>
    <body>
        <h1>Dashboard</h1>
        <p>Welcome to the dashboard, <%= user.username %></p>
        <div class="budget-form">
            <form action="/update-budget-timeframe" method="POST">
                <label for="budget">Budget:</label>
                <input type="number" id="budget" name="budget" value="<%= user.budget %>">
              
                <label for="timeframe">Timeframe:</label>
                <input type="date" id="timeframe" name="timeframe" value="<%= user.timeframe %>">
              
                <button type="submit">Save</button>
              </form> 
        </div>
        <% if (user.isAdmin) { %>
            <a href="/admin/pendingUsers">View Pending Users</a>
        <% } %>
        <a href="/logout">Logout</a>
    </body>
</html>
